/*using Godot;
using System;

public partial class GuardVisual : Sprite2D
{
	[Export] public var guard;

	public override void _Input(InputEvent @event)
	{
		if (@event is InputEventMouseButton e && !e.Pressed && e.ButtonIndex == MouseButton.Left && !selected)
		{
			if (GetRect().HasPoint(ToLocal(e.Position)))
			{
				// guard.GuardJDW.Selected(true);
			}
		}
	}
}
*/
